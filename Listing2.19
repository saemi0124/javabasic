import java.util.Scanner;

public class practice1{
	public static void main(String[] arg){
		Scanner input = new Scanner(System.in);
		System.out.println("Enter the monthly szaving account: ");
		final double interestRate = 0.00417;
		double saving = input.nextDouble();
		double total = 100 * (1 + interestRate);
		total = (100 + total) * (1 + interestRate);
		total = (100 + total) * (1 + interestRate);
		total = (100 + total) * (1 + interestRate);
		total = (100 + total) * (1 + interestRate);
		total = (100 + total) * (1 + interestRate);
		System.out.println("After the six month, the account value is $" + total);
		
	}
}
